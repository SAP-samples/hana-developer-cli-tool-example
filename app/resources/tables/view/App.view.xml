<mvc:View controllerName="sap.hanacli.tables.controller.App" height="100%" width="100%"
	xmlns:l="sap.ui.layout"
	xmlns:f="sap.ui.layout.form"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns:table="sap.ui.table"
	xmlns="sap.m">
	<ScrollContainer height="100%" width="100%" vertical="true" focusable="true">
		<f:SimpleForm id="SimpleFormToolbar" editable="true" layout="ResponsiveGridLayout" adjustLabelSpan="true" singleContainerFullSize="false" ariaLabelledBy="Title1">
			<f:toolbar>
				<Toolbar id="TB1">
					<Title id="Title1" text="{i18n>appTitle} - {i18n>appDescription}"/>
					<ToolbarSpacer />
					<Button icon="sap-icon://refresh" tooltip="{i18nReuse>gui.execute}" press="executeCmd" />

				</Toolbar>
			</f:toolbar>
			<f:content>

				<Toolbar ariaLabelledBy="Title2">
					<Title id="Title2" text="{i18nReuse>gui.target}"/>
					<ToolbarSpacer />
					<Button icon="sap-icon://settings" tooltip="{i18nReuse>gui.targetDetails}" press=".openUrl('/ui/#systeminfo-ui', true)" />
				</Toolbar>

				<Label text="{i18nReuse>scheam}" required="true" />
				<Input id="Schema" value="{/schema}" valueLiveUpdate="true" showSuggestion="true" suggest="loadSchemaFilter">
				</Input>
				<Input value="{hanaModel>/user/0/CURRENT_SCHEMA}" visible="{= ${/schema} === '**CURRENT_SCHEMA**'}" editable="false"/>

				<Label text="{i18nReuse>table}" required="true"/>
				<Input id="Table" value="{/table}" showSuggestion="true" suggest="loadTableFilter">
				</Input>

				<core:Fragment fragmentName="sap.hanacli.common.view.Connection" type="XML"/>
				<core:Fragment fragmentName="sap.hanacli.common.view.Debug" type="XML"/>

			</f:content>
		</f:SimpleForm>

		<table:Table
				id="table"
				selectionMode="MultiToggle"
				visibleRowCount="10"
				enableSelectAll="false"
				rows="{resultsModel>/rows}"
				threshold="15"
				enableBusyIndicator="true"

				ariaLabelledBy="TableTitle">
				<table:extension>
					<OverflowToolbar style="Clear">
						<Title id="TableTitle" text="{i18nReuse>gui.cmdResults}"/>
					</OverflowToolbar>
				</table:extension>
				<table:noData>
					<BusyIndicator class="sapUiMediumMargin"/>
				</table:noData>
			</table:Table>

	</ScrollContainer>
</mvc:View>